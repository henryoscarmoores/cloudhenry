# Ghost code-injection exports contain the Travelpayouts API token
# and must never reach this public repo.
footer-backup.txt
header-backup.txt
*backup*.txt
*.secret

# Travelpayouts API token - never commit
.token
fetch-fares.log
fares.json.prev
fares-slim.json
daily-refresh.log
